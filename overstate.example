sync_mod:
  match: 'web*'
  function:
    saltutil.sync_modules

mysql:
  match: 'web*'
  require:
    - sync_mod
  sls:
    - foo.db

app:
  match: 'web*'
  require:
    - mysql
  sls:
    - foo.vhost
    - foo.app

load_db:
  match: 'web*'
  require:
    - app
  sls:
    - foo.loaddata
